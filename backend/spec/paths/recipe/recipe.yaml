get:
  tags:
    - Recipes
  responses:
    "200":
      description: A list of all Recipes
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../components/schemas/recipe.yaml#/RecipeResponseModel'

